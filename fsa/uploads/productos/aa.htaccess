# Allow image files
<Files ~ "\.(jpg|jpeg|png|gif|webp)$">
    Order allow,deny
    Allow from all
</Files>

# Deny access to other files
<Files ~ "^.*$">
    Order deny,allow
    Deny from all
</Files>

# Prevent directory browsing
Options -Indexes
